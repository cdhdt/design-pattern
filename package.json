{
  "name": "facturoplus",
  "version": "1.0.0",
  "description": "Génération de documents commerciaux - Template Method + Strategy",
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "lint": "eslint . --ext .ts",
    "qa": "npm run typecheck && npm run format && npm run lint"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "prettier": "^3.9.5"
  }
}
